<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorPrimary">@color/toolbarTitleColor</item>
    </style>

    <style name="HotelListItemText" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">@drawable/hotel_item_gradient</item>
    </style>
</resources>
